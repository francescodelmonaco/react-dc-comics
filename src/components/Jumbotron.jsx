const Jumbotron = () => {
    return (
        <section className="jumbotron">
            <div className="margin-80">
                <span>CURRENT SERIES</span>
            </div>
        </section>
    )
};

export default Jumbotron;